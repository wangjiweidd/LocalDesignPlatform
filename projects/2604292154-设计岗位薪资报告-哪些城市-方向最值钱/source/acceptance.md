# 验收记录｜设计岗位薪资报告-哪些城市-方向最值钱

验收时间：2026-05-02 00:40

## 文件验收

- `source/research.md` 已更新，包含来源、边界、关键判断和内容路线。
- `source/outline.md` 已更新，包含 8 页卡片规划和假设说明。
- `source/copy.md` 已更新，包含标题、封面文案、逐页正文和小红书配文。
- `配文文稿.md` 已生成，作为发布标题和小红书正文的唯一发布源。
- `generate-cards.js` 已按 Salary Playbook 风格生成 8 页 HTML。
- `preview/index.html` 已生成，可直接打开预览整套卡片。
- `html/` 已生成 8 个单页 HTML 和 `index.html`。
- `png/` 已生成 8 张 PNG。

## 视觉验收

- 画布：8 张 PNG 均为 `1080x1440`。
- 风格：米白底、黑色硬边框/投影、红蓝强调、纯文字信息卡、底部红色结论条。
- P1 封面：大标题保留两行以上换行，未压成单行。
- P3、P8 已做重点抽查，底部红条没有被导出裁切，内容没有明显遮挡。
- 低密度页已补充可用模块，P8 增加投递、报价、作品集、换城市检查项。

## 内容验收

- 标题线程可连读：平均数误导 -> 城市差距 -> 城市选择 -> 方向上限 -> 年限证据 -> 作品集证据 -> 查薪避坑 -> 跳槽复核。
- 每页正文都包含可用单位：城市问题、城市表、方向判断、经验证据、作品集对照、查薪误区、JD 复核动作、跳槽检查项。
- `配文文稿.md` 开头直接下判断，没有“这组图主要讲/本文将介绍”式介绍句。
- 已检索禁用表达：未命中 `真正`、`看完`、`本文将`、`保姆级`、`必看`、`本质上`、`赋能`、`闭环`、`底层逻辑` 等高风险词。

## 命名验收

- 正式生产目录使用 `260429_设计岗位薪资报告-哪些城市-方向最值钱`。
- 未添加 `_done`。
- HTML 与 PNG 文件名一一对应。

## 残余风险

- 原始资料未提供可审计招聘样本，研究中已标注假设和数据边界。
- 如果发布时需要写精确薪资数字，建议追加 30-50 条目标城市 JD 做二次统计。
