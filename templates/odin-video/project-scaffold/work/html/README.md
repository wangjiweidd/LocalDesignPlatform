# HTML Preview

HyperFrames HTML animation files live here.

Default behavior:

- Generate or update HTML first.
- Run HyperFrames checks before presenting the preview.
- Do not render MP4 until the user confirms.

